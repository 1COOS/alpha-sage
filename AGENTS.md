# Alpha Sage 工程约束

- 不生成 Demo 行情、持仓、策略成绩或虚构成交。
- 任一关键数据、模型、日历、来源或版本缺失时必须阻断，不得静默降级为猜测执行。
- 现金是合法组合结果；不得为了满足持仓数量而强制买入。
- 不接真实券商，不增加杠杆、做空、北交所或非 A 股 V1 能力。
- 市场差异必须通过 `MarketAdapter` 和带生效日的规则版本扩展。
- 决策、证据、研究、成交、经验、模型调用和审计数据保持追加式；不要原地改写历史事实。
- 模型不得绕过硬风控、手动启用、挑战者影子期、人工批准或回滚流程。
- 不引入微服务、消息队列、外部向量库、因子平台或运行时代码生成执行链。
- 修改后至少运行 `make check`；涉及迁移时还要在真实 SQLite 文件上执行 `alembic upgrade head` 和 preflight。
